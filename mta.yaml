ID: SAC01
_schema-version: '2.1'
version: 0.0.2

modules:
 - name: DB
   type: hdb
   path: DB
   requires:
    - name: hdi_DB

resources:
 - name: hdi_DB
   parameters:
      config:
         schema: HSB
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
